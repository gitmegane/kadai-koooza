<% content_for :cover do %>
  <div class="cover">
    <div class="cover-inner">
      <div class="cover-contents">
        <h1>素敵な講座と出会う場所</h1>
          <% unless logged_in? %>
            <%= link_to 'KOOOZAを始める', signup_path, class: 'btn btn-success btn-lg' %>
          <% end %>
          
      </div>
    </div>
  </div>
<% end %>

<%= render 'courses/courses', courses: @courses %>
<p>テスト</p>
